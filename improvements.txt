Namming : load_clubs, load_competitions on snake_case
utils.py :
- Sépare la logique de chargement des données du reste du code.
- Les fichiers JSON sont chargés une seule fois au démarrage.
- current_app.clubs / current_app.competitions évitent de charger les fichiers plusieurs fois.

Files organization
/mon_projet/
│── app/              
│   ├── __init__.py       # Initialisation de l'application
│   ├── routes.py         # Routes de l'application
│   │── utils.py          # Fonctions utilitaires comme loadClubs() et loadCompetitions()
│   ├── templates/        # Templates HTML
│   ├── static/           # CSS, JS, images
│── data/                 # Fichiers JSON
│── tests/                # Dossier des tests
│── config.py             # Configuration de l'application
│── run.py                # Point d'entrée de l'application
│── requirements.txt      # Dépendances
│── .gitignore            # Fichiers à ignorer

Lorsque tu définis une route dans un Blueprint, Flask ajoute automatiquement le nom du Blueprint comme préfixe à l’endpoint.

🔹 Correction dans welcome.html et def logout():
Ajout de "main." pour faire référence au Blueprint
- url_for("main.logout")
- url_for("main.book")

Issues

1. ERROR: Entering a unknown email crashes the app

Initial
@main.route("/showSummary", methods=["POST"])
def showSummary():
    club = [club for club in clubs if club["email"] == request.form["email"]][0]
    return render_template("welcome.html", club=club, competitions=competitions)

add flash message if email not found and display on index.html

Initial
@main.route("/book/<competition>/<club>")
def book(competition, club):
    foundClub = [c for c in clubs if c["name"] == club][0]
    foundCompetition = [c for c in competitions if c["name"] == competition][0]
    if foundClub and foundCompetition:
        return render_template(
            "booking.html", club=foundClub, competition=foundCompetition
        )
    else:
        flash("Something went wrong-please try again")
        return render_template("welcome.html", club=club, competitions=competitions)